package org.example;

public class Main {

    public static void main(String[] args) {

        // 문자열 { 1 + 2 * 3 } 항상 정상적인 규격으로 값이 들어온다는 조건!
        // 문자열 쪼개서 배열로 변환
        // 배열을 스택에 넣는다 (뒤부터)
        // 스택에서 3개씩( 숫자, 식, 숫자 형식)꺼내어 pop 계산
        // if 스택에 빈값이 없을 때 결과 출력 return
        // 다시 스택에 넣는다 push

    }
}
